// vite.config (root)
import baseConfig from './public/vite.config.js';

export default {
    ...baseConfig,
    // Overrides hier
    server: {
        ...baseConfig.server,
        port: 4000 // Andere port voor dev
    }
};